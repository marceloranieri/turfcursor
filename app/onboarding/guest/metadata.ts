import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Guest Mode | Turf',
  description: 'Try Turf without signing up',
  robots: {
    index: false,
    follow: false,
  },
}; 