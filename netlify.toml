[build]
  base = "turf-app/"
  publish = "out"
  command = "npm install --legacy-peer-deps && npm run build"

[build.environment]
  NODE_VERSION = "18"
  
[dev]
  command = "npm run dev"
  port = 3000
  publish = "out" 