name: Vercel Production Deployment

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

env:
  NEXT_PUBLIC_APP_URL: https://app.turfyeah.com
  NEXT_PUBLIC_SUPABASE_URL: https://xwgsbhncprilsuczqmjr.supabase.co
  NEXT_PUBLIC_SUPABASE_ANON_KEY: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Inh3Z3NiaG5jcHJpbHN1Y3pxbWpyIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NDQxMjg4MTIsImV4cCI6MjA1OTcwNDgxMn0.UxCdhTb2Ti0wauZkcIQbWQRg0xNW6_spJ3Hm9rjHPms
  NEXT_PUBLIC_GOOGLE_CLIENT_SECRET: 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
  PUBLIC_GITHUB_CLIENT_ID: Ov23liA75TUxFl1WynfC
  GITHUB_CLIENT_SECRET: 83ad9ab633a07fbdf1b206224251c822ad37a923
  GIPHY_API_KEY: LAEhUpkO8Kb5pCjPgiufyX2LVJDv01s6
  VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
  VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
  VERCEL_PROJECT_ID: prj_FoBSdmqcM5MPfFjAK4SDFt3kjnHc

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run type check
        run: npm run type-check

      - name: Run linting
        run: npm run lint

      - name: Run tests
        run: npm test

  deploy:
    needs: validate
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install Vercel CLI
        run: npm install --global vercel@latest

      - name: Pull Vercel Environment Information
        run: vercel pull --yes --environment=production --token=${{ env.VERCEL_TOKEN }}
        env:
          VERCEL_ORG_ID: ${{ env.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: prj_FoBSdmqcM5MPfFjAK4SDFt3kjnHc

      - name: Build Project Artifacts
        run: vercel build --prod --token=${{ env.VERCEL_TOKEN }}
        env:
          VERCEL_ORG_ID: ${{ env.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: prj_FoBSdmqcM5MPfFjAK4SDFt3kjnHc

      - name: Deploy to Vercel
        run: vercel deploy --prebuilt --prod --token=${{ env.VERCEL_TOKEN }}
        env:
          VERCEL_ORG_ID: ${{ env.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: prj_FoBSdmqcM5MPfFjAK4SDFt3kjnHc 