{
  "buildCommand": "cd turf-app && npm install --legacy-peer-deps && npm run build",
  "outputDirectory": "turf-app/out",
  "framework": "nextjs",
  "installCommand": "echo 'Skipping install command'",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ],
  "rootDirectory": "turf-app"
} 